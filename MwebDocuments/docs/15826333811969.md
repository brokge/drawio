# 批量插入

```sql
# batchInsert 
 insert into hpp_fire_member_invite_code    (id,invite_code,usable_status,invite_code_length,create_time, op_time, is_valid,last_ver)
    values (),(),();

# selectAll
select invite_code from hpp_fire_member_invite_code  where invite_code_length = 6;
```
